# 设计思想

- 反线性瀑布：要求每个功能模块都提供事件驱动的接口，而非串行调用链；明确要求使用 '@router' 和条件路由，禁止在代码中硬编码阶段顺序。
- 反伪契约：强制所有 Agent 输出遵循 Pydantic 模型定义，要求 DSPy Signature 作为函数签名的一部分，并提供运行时校验失败的重试/降级逻辑。
- 反人工瓶颈：规定状态流转必须通过 'ProjectState' 对象，禁止在各 Crew 间直接传递裸字典；要求所有流程支持基于 LangGraph 检查点的断点续行，并提供序列化方案。
- 反Token灾难：要求上下文组装模块必须实现 'assemble_context' 接口，严格遵循四层记忆架构与 token 预算（4K-8K），禁止一次性注入全量历史文本。
- 反虚伪多智能体：强调不同角色 Agent 必须使用独立 LLM 实例和温度参数（高温/低温分工），不允许在单次调用中切换 system prompt 来冒充多角色。
- 反静态规划：大纲数据模型必须支持 'blank' 节点状态和运行时动态插入、分支操作；结构修改必须触发影响分析，而非静默改写。
- 反回滚毁灭：所有修改操作必须执行 Neo4j 依赖图遍历，返回三级影响标记列表，并提供局部重写建议，禁止直接全量回滚。
- 反锚点空洞：持久化层必须实现属性图模型，节点必须包含 'content_hash', 'lock_status' 等元数据，并提供 Cypher 查询模板。
- 反制造业隐喻：校验引擎必须输出连续置信度分数，流程门控采用阈值区间（80/60 分界），严禁使用 'if pass else fail' 二元判定。
- 反认知独裁：交互层必须实现差异化的审批视图（交通灯+异常聚焦），高置信操作自动通过但需记录审计日志，不可每步都强制人工确认。


# 核心原则

- **高内聚、低耦合**

- 保持真实代码的纯净，不引入任何 MOCK 数据。当且仅当在测试需要时，才引入 MOCK 数据。
- 调用 DeepSeek API 必须从外部导入已经封装好的接口函数，禁止在业务代码中直接调用 API。
- 禁止为了快速修复错误而降级功能。除非受限于环境无法实现，否则需要 100% 落地功能设计文档的要求。
- 在通常情况下，不需要兼容以前的错误实现。追求以最优雅的方式实现设计文档的要求。
- 每次提交时，除非明确命令，否则仅提交本次修改过的文件，以免与其他 Agent 的提交冲突。
- 优先使用项目已有的日志模块，而非自定义打印语句，以保持日志输出的一致性和可管理性。
- 每次自动提交时，仅提交自己修改过的文件，避免提交其他 Agent 的未完成代码，保持代码库的整洁和稳定。
- 尽可能让代码优雅。
- 前端代码，要做到**逻辑层**和**表现层**分离，禁止在 React 组件中直接调用 API 或处理复杂业务逻辑。所有数据获取和状态管理应通过自定义 Hooks 或 Context 实现，组件仅负责渲染和用户交互。

# 设计文档

1. 技术栈设计：`docs\写作助手-技术栈设计.md`
2. 项目结构设计：`docs\写作助手-项目结构.md`
3. 注释规范：`docs\注释规范.md`