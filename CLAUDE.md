# 项目说明

本项目官方名称为：**双摆非线性动力学虚拟仿真实验**

## 项目规范位置

> 在进行项目开发前，请务必阅读以下规范文件，以确保项目的顺利进行：

1. 技术栈：@docs\双摆混沌实验室-技术栈设计.md
2. 项目结构：@docs\双摆混沌实验室-项目结构.md
3. 功能设计：@docs\功能设计\原始材料\ （选读）

## 核心原则

- **高内聚、低耦合**

- 保持真实代码的纯净，不引入任何 MOCK 数据。当且仅当在测试需要时，才引入 MOCK 数据。
- 调用 DeepSeek API 必须从外部导入已经封装好的接口函数，禁止在业务代码中直接调用 API。
- 禁止为了快速修复错误而降级功能。除非受限于环境无法实现，否则需要 100% 落地功能设计文档的要求。
- 在通常情况下，不需要兼容以前的错误实现。追求以最优雅的方式实现设计文档的要求。
- 每次提交时，除非明确命令，否则仅提交本次修改过的文件，以免与其他 Agent 的提交冲突。
- 优先使用项目已有的日志模块，而非自定义打印语句，以保持日志输出的一致性和可管理性。
- 每次自动提交时，仅提交自己修改过的文件，避免提交其他 Agent 的未完成代码，保持代码库的整洁和稳定。
- 尽可能让代码优雅。
- 前端代码，要做到**逻辑层**和**表现层**分离，禁止在 React 组件中直接调用 API 或处理复杂业务逻辑。所有数据获取和状态管理应通过自定义 Hooks 或 Context 实现，组件仅负责渲染和用户交互。

# 特殊指令

- 统一使用**中文**回复用户、进行提交等操作。
- 计划模式下，必须用中文输出计划。